interface:
  display_name: "Review Invariants"
  short_description: "Run bounded contract-driven code review"
  default_prompt: "Use $review-invariants to review this change without starting an unbounded loop."
